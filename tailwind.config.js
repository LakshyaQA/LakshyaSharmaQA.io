/** @type {import('tailwindcss').Config} */
// Tailwind v4 — theme config (colors, fonts, animations) has moved to src/index.css @theme block
export default {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  darkMode: 'class',
}
